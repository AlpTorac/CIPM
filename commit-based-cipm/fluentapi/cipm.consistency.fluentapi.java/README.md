# Fluent API for Java

This plug-in contains the implementation of the fluent API generation for Java and considers the (EMF-based) JaMoPP metamodel for Java ... TODO: Link the JaMoPP metamodel. The structure of this plug-in reflects the structure of the base plug-in for fluent API generation ... TODO: Link the base fluent api plug-in readme. The fluent API class within this plug-in is `cipm.consistency.fluentapi.java.api.FluentJavaAPI.java` TODO: Link the file.

Note that the 'layout' package and the relevant JaMoPP features are excluded in this implementation, in order to keep the generated fluent api code to the metamodel elements that are directly related to Java.

For exemplary usage, refer to the test package of this plug-in ... TODO: Link the test package. For further details, refer to the base plug-in for fluent api generation ... TODO: Link the base fluent api plug-in readme
